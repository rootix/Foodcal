import { Routes } from '@angular/router';
import { RecipesViewComponent } from './views/recipes-view/recipes-view.component';

export const routes: Routes = [{ path: '', component: RecipesViewComponent }];
